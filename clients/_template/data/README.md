# Client data

Put the CSVs, exports and spreadsheets the client shares here.

Agents read this folder and **run real queries** over whatever they find. Anything not here
and without a credential declared in `client.yaml -> tools` is declared as a hole in the
deliverable — never silently estimated.
